REDIS_URL =
NEWS_API_KEY =
NEBIUS_API_KEY =
# Comma-separated list of origins allowed to make credentialed requests.
# Unset (or "*") means no cross-origin credentialed access, since
# allow_credentials=True is enabled. For local development, either set
# this explicitly or set ENABLE_DEV_CORS_ORIGINS=1 to allow
# http://localhost:3000 and http://127.0.0.1:3000.
CORS_ALLOWED_ORIGINS =
ENABLE_DEV_CORS_ORIGINS =