MISCONCEPTIONS = {
"pre_increment_confusion": {
"description": "Student assumes variable increments before the print statement in a while loop.",
"keywords": ["increment", "print", "order", "while", "before"]
},
"off_by_one": {
"description": "Student uses < instead of <= or vice versa, causing one extra or missing iteration.",
"keywords": ["off by one", "range", "loop", "boundary", "extra"]
},
"infinite_loop": {
"description": "Student forgets to increment the loop variable, causing an infinite loop.",
"keywords": ["infinite", "never ends", "no increment", "while true"]
}
}
